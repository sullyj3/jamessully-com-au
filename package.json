{
  "name": "james-sully-com-au",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm run build",
    "dev": "cd client && npm run dev",
    "start": "cd client && npm run start",
    "postinstall": "cd client && npm run postinstall"
  }
}
